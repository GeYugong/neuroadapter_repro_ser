# E1 Stimulus Manifest

The generated manifest is based only on ground-truth NSD stimuli. Reconstruction
outputs are never used for category selection.

Face, Body, and Scene are intended as confirmatory categories after audit.
Word is exploratory until an OCR-backed manifest contains enough valid images.

